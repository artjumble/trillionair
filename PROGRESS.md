# TRILLIONAIRE — Progress Log

> One line per `/loop` iteration: `YYYY-MM-DD — what shipped — <short commit hash>`.
> A fresh, memory-less loop run reconstructs state from here + ROADMAP.md.

- 2026-06-27 — Bootstrap: GOAL.md, ROADMAP.md, scaffold, vendored break_infinity.js, GitHub Pages deploy workflow, first push — (initial commit)
- 2026-06-27 — Workers'-share counter (Phase 6 #1): added state.workersPaidTotal (Decimal, persisted), accumulated from wagesPerSec each tick, shown as a running wage-red line under the wage breakdown — "Workers earned, all-time (everyone, combined): $X". It tracks ~your generator earnings early, then stalls as you cut wages. Verified in-browser: accumulates correctly ($2.26 at $0.12/s over ~18s), persists, displays once wages start; no console errors. (Sets up the #3 dark-turn reveal next.)
- 2026-06-27 — Reframed the "honest" counter (user feedback): the old "$1/second" was actually $31.5M/yr — absurdly high to call honest. Rebased it on a genuine high earner, $1,000,000/year ($0.0317/sec), and rewrote the copy to land the point: even at a million a year — untaxed, unspent — a trillion is 1,000,000 years away (all of recorded history ≈ 5,000). Verified in-browser: rate exact ($0.03169/s), displayed value matches playSeconds × rate, copy renders, no console errors.
- 2026-06-27 — Phase 6 bug fix: locked Shortcuts no longer show with enabled buttons. Root cause: renderUpgrades set card.hidden=true for locked upgrades, but `.upgrade { display: flex }` overrode the [hidden] attribute's display:none (equal specificity, author rule wins), so all 18 cards stayed visible with default-enabled buttons until "unlocking" flipped them to disabled. Fix: one CSS rule `.upgrade[hidden] { display: none; }`. Verified in-browser: at $0, 0 upgrade cards shown (was 18); at $60 lifetime Firmer Handshake appears DISABLED (cost $100); at $105 it's enabled + highlighted. No console errors.
- 2026-06-27 — Phase 6 bug fix: buttons now enable exactly when shown money meets shown cost (was an off-by-one — displayed money rounded UP while non-integer generator costs were checked exactly). Fix: format() now floors displayed values (never overstate what you have), and generator unitCost/bulkCost ceil to whole dollars so the shown price, the affordability check, and the charge all agree. Verified in-browser: at $18 money / $18 cost the button is enabled and the buy succeeds (1→2 owned, paid $18, next cost $20); no console errors.
- 2026-06-27 — Phase 6 balance: made the no-prestige grind to $1T much longer. Diagnosed via simulation that cost-curve and milestone tweaks were nearly inert (exponential income compounding dominates) — the real lever is the income/cost ratio. Reverted those experiments (restored 1.15 curve + ×2 milestones) and added a single global INCOME_SCALE = 0.25 damper applied in recomputeIncome + the rate display. Sim: no-prestige (no upgrades) $1T went 3.4h → 13.2h (~4×); prestige/upgrade multipliers stack on top to restore pace, making Old Money resets the real path. Verified in-game: 1 lemonade now $0.03/s (= 0.10×0.25), boots clean, no console errors.
- 2026-06-27 — Phase 6 bug fix: Shortcuts now have clear used/available indicators. Purchased shortcuts no longer vanish — they stay visible, dimmed, with a green ✓ and an "✓ Active" button. Affordable ones get a gold accent border + a gentle pulse so they stand out; unlocked-but-unaffordable stay muted; still-locked stay hidden. Verified in-browser (normal + dark-turn modes): available border = accent gold, owned card opacity 0.6 + "✓ Active", states toggle correctly on purchase; no console errors.
- 2026-06-27 — Phase 6: expanded the comparison ticker from 7 to 13 verified equivalents — added median U.S. rent (~$1,500/mo), in-state public university (~$11K/yr), new cars (~$48K), median U.S. homes (~$420K), a median worker's lifetime earnings (~$1.7M), and NASA's annual budget (~$25B/yr) — filling the gaps between school lunches and ending world hunger. Each cites its figure transparently. Verified renders/math at $10T (6.67B months of rent, 23.81M homes, 400 years of NASA's budget); no console errors.
- 2026-06-27 — Phase 6 (post-release): tabbed the UI. The HUD (money, income, wage breakdown, click, goal bar, comparison, honest counter) stays fixed; a tab bar switches between Businesses / Shortcuts / Inheritance / Achievements / Spend It / Settings. Spend It tab stays hidden until $1T (tab system owns panel visibility; renderSpend just toggles the tab button). Proper role=tab/tablist/tabpanel + aria-selected. Verified in-browser: default Businesses active, switching shows/hides the right panels with aria updated, Spend It reveals post-$1T, no console errors.
- 2026-06-27 — **PROJECT COMPLETE.** Polished the README (feature overview, how-to-play, satire table, build story, deploy notes) and ran the final §6 pass. Smoke test on a fresh game: clean load, no console errors, core loop works (clicks → buy → income), and every system present — 10 generators, 18 upgrades (incl. 6 wage cuts), 13 achievements, 7 meta-upgrades, 9 luxuries, honest counter, comparison ticker, wage breakdown + worker pleas, prestige/inheritance, settings, mute, and the (pre-$1T hidden) Spend It panel. All five phases done: MVP → Depth → Prestige-as-inheritance → Satire systems → Juice/sound/endgame/polish. The trillion-dollar critique lands through both mechanics and copy. Trillionaire is finished and playable end-to-end.
- 2026-06-27 — Final balance tuning: ran a headless greedy-play simulation of the real generator/milestone economy. Worst-case (generators + milestones + early clicking only, NO upgrades/prestige/offline): $1M 1.4h, $1B 2.4h, $100B 3.1h, $1T 3.4h. Owned forms a clean pyramid (179 lemonade → 6 top-tier — every tier used), the greedy buyer never stalled (no walls), and the curve accelerates at the end. With upgrades (×3/gen, ×2–3 global), achievements, prestige and meta-multipliers layered on, real time-to-$1T is ~1–3h — fast enough to reach the dark-turn/endgame payoff, while the honest counter keeps it feeling absurd. Conclusion: economy is sound; no numeric changes warranted (YAGNI).
- 2026-06-27 — Accessibility pass: visible :focus-visible outlines on all controls; Escape closes any open modal and the close button is focused on open; aria-pressed on the mute and ×1/×10/Max toggles (updates live); the $1T bar is a role="progressbar" with live aria-valuenow; reduced-motion already disables particles/shake/transitions. Verified in-browser: aria states correct and dynamic, Escape closes a modal, goal-bar value updates, no console errors. (Contrast: --ink-dim on the dark bg clears WCAG AA.)
- 2026-06-27 — Settings panel: scientific-notation toggle (wired into format.js), the existing mute, and a two-click hard reset. Caught and fixed a real bug — hardReset removed the save but the reload's beforeunload handler re-saved the in-memory state, resurrecting it; added a `wiped` flag that no-ops save() after a reset. Verified in-browser: money toggles $79.00T ↔ $7.90e+13, hard reset wipes to a fresh $0 game (dark turn cleared, spend panel hidden), no console errors.
- 2026-06-27 — The unspendable "Spend It" endgame (new js/luxuries.js): unlocks after the dark turn; 9 absurd luxuries (a Senator, a Newspaper, a Pledge to End Hunger you don't keep, a Space Program…) you can buy repeatedly. Tracks total flung away and an "emptied %" bar that stays near zero — income refills faster than you can spend. Verified in-browser: $100B on 5 space programs moved the bar to 0.13%, spent/owned/affordability exact, panel hidden until $1T; 2,740-years-at-$1M/day figure confirmed; no console errors.
- 2026-06-27 — The curdling dark turn at $1T: crossing a trillion fires once — a hollow-victory modal ("You earned it. (You didn't.) The confetti isn't coming…"), the palette drains to grey (theme variables overridden, plus a grayscale filter where supported), the click sound sours to a low detuned sawtooth, and the screen shakes. Persisted (reachedTrillion) so it stays curdled and the modal never repeats. Verified in-browser: modal + shake on first crossing, money color green→grey (rgb(154,154,154)), no repeat modal on reload, no console errors. (Headless preview ignores CSS filter, so the drain rides on variable overrides — robust either way.)
- 2026-06-27 — Sound (new js/sound.js, WebAudio, no asset files): pitch-randomized "cha-ching" on click that rises with wealth magnitude, a rising arpeggio on purchases, and a persisted mute toggle (🔊/🔇) in the header. AudioContext created lazily on first gesture; all calls no-op when muted/unavailable. Verified in-browser: click path creates AudioContext without error, mute toggles and persists across reload, no console errors. (Audio isn't audible in the headless test env; code path verified.)
- 2026-06-27 — Phase 5 start: juice. Coin-burst particles on every click (6, fly outward and fade), a springy button squash-stretch (pop), and a milestone screen-shake fired on "you didn't build this" reveals. All respect prefers-reduced-motion; particles clean up via animationend + timeout fallback (no leak). Verified in-browser: 6 particles per click cleaning to 0, pop class applied, #app shakes on reveal, no console errors.
- 2026-06-27 — "You didn't build this" reveals (closes Phase 4; new js/reveals.js): one-time milestone messages at $1M/$100M/$1B/$100B/$1T that fire as prominent, lingering toasts puncturing the self-made myth (up to "A trillion dollars. You didn't build this — nobody could. It was gathered, a dollar at a time, from everyone else."). Tick-based detection, persisted so each fires once. Verified in-browser: all 5 fired at $79T with correct copy, did not repeat after reload, no console errors.
- 2026-06-27 — Comparison ticker (new js/comparisons.js): the single teacher annotation became a ticker rotating every 4.5s through 7 verifiable equivalents (school lunches ~$4, a year above the $2.15/day poverty line, federal-minimum-wage years, teachers ~$69K, household income ~$80K, nurses ~$86K, and the ~$40B/yr UN end-world-hunger figure), each scaled live and cited transparently; cycles only through currently-affordable framings. Verified in-browser at $79T: school lunches 19.75T, teachers 1.14B, household-years 987.5M — all exact; no console errors.
- 2026-06-27 — Worker-begging beat: an escalating, unsettling worker plea appears below the wage breakdown once you start cutting wages and deepens as the rate drops (mild "is this schedule a mistake?" → "I have kids" → "It used to have names attached" at 98% kept). Thresholds at the midpoints between wage steps so float error in repeated subtraction can't shift a beat. Verified in-browser: pleas mapped correctly across 0.15/0.10/0.02 with no off-by-one; no console errors.
- 2026-06-27 — Cut-wages upgrade branch: 6 wage-cut upgrades (Performance Review, Offshore, Reclassify as Contractors, Bust the Union, Automate, Unpaid Internships) that each lower the wage rate (0.30→0.02). wageRate is now derived from purchased cuts. Verified in-browser: buying −5% kept gross fixed ($2.61K/s), dropped wages $783→$653, raised "you keep" 70%→75% and net income $1.83K→$1.96K; card hid on purchase; no console errors. The mechanic: your growth comes from suppressing the wage line.
- 2026-06-27 — Phase 4 start: wages model. Income now reads as workers producing gross output, you paying a wage share (default 30%), and keeping the rest — shown explicitly ("workers produce $X/s · you pay −$Y/s in wages · you keep 70%"). Anchored so the default rate leaves net income unchanged (no nerf), setting up cut-wages upgrades as a pure win for you / loss for them. Caught and fixed a temporal-dead-zone bug (state object referenced DEFAULT_WAGE_RATE before its const) that had broken boot; verified clean reload, breakdown math (2.43 gross − 0.73 wages = 1.70 net), no console errors.
- 2026-06-27 — "New generation" framing (closes Phase 3): a generation counter (persisted, increments on cash-out) and a celebratory modal that reframes the reset as inheritance — "hand the empire to your heirs. Generation N begins already holding X Old Money (×M income before you lift a finger). They did nothing to earn it. Neither did you." Panel shows "Gen N". Verified in-browser: prestige with +8 gain → Generation 2, modal head-start ×3.80 = prestige(1.9)×Trust Fund(2), closes cleanly, no console errors.
- 2026-06-27 — Meta-upgrade tree (new js/metaupgrades.js): 7 permanent upgrades bought with Old Money that survive every reset — income/click multipliers, "Born on Third Base" starting cash, and a "Generational Compounding" synergy. Added an earned-vs-spendable Old Money split (prestigeEarned) so spending can't be re-farmed by re-prestiging. Verified in-browser: Trust Fund (×2 income) made displayed income exactly $0.99 = 4×0.10×1.12(ach)×1.10(prestige)×2(meta); card marked Inherited; balance 2→1 while earned stayed 2; no console errors.
- 2026-06-27 — Phase 3 start: prestige-as-inheritance core (new js/prestige.js). "Old Money" currency = floor(cbrt(lifetimeEarned/$1M)); each grants +10% income forever. Inheritance panel previews the gain; a two-click "Go Public" cashes out — wiping money/generators/upgrades, keeping Old Money. Verified in-browser: preview showed +2 from ~$24M lifetime; confirm-click reset money/owned/upgrades to 0 and banked 2 Old Money (+20%); a post-reset lemonade earned $0.13/s = 0.10×1.12(achievements)×1.20(prestige); no console errors.
- 2026-06-27 — Offline progress + welcome-back modal (closes Phase 2): on load, credits income/sec × secondsAway × 0.5, capped at 8h, with a modal that twists the knife ("While you did absolutely nothing, your money earned $X. That's the whole trick, isn't it?"). Verified in-browser: 2h away → exact $4.60M credited; 20h → capped to 8h ($18.74M) with the cap note; Collect button dismisses; no console errors.
- 2026-06-27 — Achievements (new js/achievements.js): 13 achievements with satirical copy (Billionaire — "now you can afford opinions"; Patriot — 0% tax rate). Each earned grants +2% global income (the bonus is itself the joke). Tick-based detection, panel with earned/locked badges + count, earn toasts. Persisted in save/load. Verified in-browser via a MutationObserver: crossing 100 owned awarded "Empire" (4→5/13), badge lit, a live "Achievement Empire" toast appeared, income bonus applied, no console errors.
- 2026-06-27 — Multiplicative upgrade shop (new js/upgrades.js): 12 one-time upgrades across click/per-generator/global multipliers, with satirical names (Regulatory Capture, "Wage Review (Downward)", Effective Tax Rate: 0%). Unlock gating by owned-count or lifetime earnings; build-once cards that hide on purchase; multipliers feed recomputeIncome/recomputeClick; persisted in save/load. Verified in-browser: gen upgrade gave exact +$13.2/s, click upgrade gave ×2 clicks, cards hide on buy, purchases + income boost survive reload, no console errors.
- 2026-06-27 — Milestone bonuses: owning 25/50/100/150/200/300/400/500 of a generator each doubles its output (cumulative ×2^reached). Effective per-unit rate and "next at N" progress shown per row. Verified in-browser: 33-owned generator shows ★×2 and $0.20/sec each, sub-25 shows "bonus at 25", income recompute uses the same multiplier, no console errors.
- 2026-06-27 — Phase 2 start: added 6 more generator tiers (4→10), continuing the satirical arc — Hedge Fund, Private Equity, Offshore Tax Haven, Media Empire, Lobbying Operation, Too-Big-to-Fail Bank — each ~15× the last with rising efficiency. Verified in-browser: all 10 render with correct names/costs (top tier $500B), no console errors; integrates via the existing dynamic render/state/save paths.
- 2026-06-27 — Removed the "under construction" banner and **closed the Phase 1 MVP gate**. Verified clean load (no console errors) with the full core loop present and working: click→earn, 4 idle generators on the 1.15 curve, ×1/×10/Max buy, live income/sec, $1T goal bar, honest-labor counter, and wealth-comparison annotation. The game is now genuinely playable end-to-end. Next: Phase 2 (depth).
- 2026-06-27 — MVP balance pass: retuned the 4 generators (Food Truck 200/$2s, Sweatshop 3000/$40s, Rent-Seeking LLC 45000/$800s) so per-dollar efficiency *increases* up the chain (paybacks 150s→100s→75s→56s) — climbing now rewards. Verified in-browser: income deltas exact (+0.10/+2/+40/+800), each purchase visibly moves income/sec, goal bar ~0% at $151K (absurdly far, as intended), no console errors.
- 2026-06-27 — Wealth-comparison annotation: live line under the money number translating the pile into average U.S. teacher salaries (~$69K, shown transparently), with an early-game sting ("…not yet one teacher's annual salary"). Verified in-browser: not-yet/singular/plural branches all correct, $1T = 14,492,753 teachers, no console errors.
- 2026-06-27 — Buy ×1/×10/Max controls: global buy-mode selector; geometric-sum `bulkCost` and an inverted-sum `maxAffordable` with a floating-point guard that never overspends. Buttons show live count + cost per mode. Verified in-browser: ×10 cost/owned exact, Max bought the right count and left money positive, no console errors.
- 2026-06-27 — Time-betrayal "honest labor" counter: a muted $1/play-second ticker beside the booming wealth, with the "31,688 years to a trillion" gut-punch copy. Tracks `playSeconds` in state (persisted). Verified in-browser: ticks at $1/sec, correct copy, right placement, no console errors. (Also gitignored the scheduler lock file.)
- 2026-06-27 — Idle generators vertical slice: 4 satirically-themed generators on the 1.15^owned curve (data-driven), generator UI with buy-x1 + grey-out-when-unaffordable, passive income/sec wired into the tick. Reconciled scaffold-delivered Phase 1 items (state, formatting, click+float, goal bar). Fixed a floating-`+$` DOM-node leak with a timeout fallback. Verified in-browser: no console errors, exact cost/income math, passive accrual.
